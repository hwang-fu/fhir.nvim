[package]
name = "fhir-core"
version = "0.1.0"
edition = "2024"

[dependencies]
regex-lite = "0.1.9"
rust_decimal = { version = "1.42.0", features = ["maths"] }
serde_json = { version = "1.0.150", features = ["arbitrary_precision"] }

[dev-dependencies]
roxmltree = "0.21.1"
